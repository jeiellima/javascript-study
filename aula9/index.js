//String
const nome = 'Jeiel'; // '' "" ``  dentro de todos esses exemplos são Strings!

//Number
const number = 10;
const number1 = 10.50;

/* Undefined - não aponta pra nenhum local na memória 
(definido automaticamente pelo js) */
let nomeAluno;

//Nulo (null) - não aponta pra nenhum local na memória (eu que defino)
const sobrenomeAluno = null;

//Boolean - true/false
const aprovado = false;

//Array
const a = [1, 2];
const b = a;

console.log(a,b)